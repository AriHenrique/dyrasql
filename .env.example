# Configuração do DyraSQL Core - Pesos do Algoritmo de Decisão
# Score = w1xfv + w2xfc + w3xfh
# Onde:
#   - w1 = peso do fator volume (fv)
#   - w2 = peso do fator complexidade (fc)
#   - w3 = peso do fator histórico (fh)
# Os pesos devem somar aproximadamente 1.0

# Pesos dos fatores (valores entre 0.0 e 1.0)
DYRASQL_WEIGHT_VOLUME=0.7
DYRASQL_WEIGHT_COMPLEXITY=0.2
DYRASQL_WEIGHT_HISTORICAL=0.1

# Thresholds para seleção de cluster (valores entre 0.0 e 1.0)
# Score < ECS_THRESHOLD → cluster ECS
# ECS_THRESHOLD <= Score <= EMR_STANDARD_THRESHOLD → cluster EMR Standard
# Score > EMR_STANDARD_THRESHOLD → cluster EMR Optimized
DYRASQL_ECS_THRESHOLD=0.3
DYRASQL_EMR_STANDARD_THRESHOLD=0.5

# Exemplo de ajuste para distribuir melhor entre clusters:
# Se muitas queries estão indo para EMR Standard, você pode:
# - Aumentar DYRASQL_WEIGHT_VOLUME (mais peso no volume de dados)
# - Diminuir DYRASQL_ECS_THRESHOLD (mais queries vão para ECS)
# - Aumentar DYRASQL_EMR_STANDARD_THRESHOLD (menos queries vão para EMR Standard)

AWS_REGION=us-east-1
AWS_PROFILE=default

DYNAMODB_TABLE=dyrasql-history

S3_BUCKET=<bucket-name>
S3_PREFIX=<s3-prefix>/<folder>/

TRINO_ECS_URL=http://trino-ecs:8080
TRINO_EMR_STANDARD_URL=http://trino-emr-standard:8080
TRINO_EMR_OPTIMIZED_URL=http://trino-emr-optimized:8080

DYRASQL_CORE_URL=http://dyrasql-core:5000

LOG_LEVEL=INFO